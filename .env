# Arquivo: .env (Versão Final para o Docker Compose)

# Variáveis de Conexão do Banco de Dados (usadas pelo container app e migrate)
# O host 'db' é o nome do serviço do container PostgreSQL no docker-compose.yml.
DATABASE_URL=postgresql://<seu_usuario>:<sua_senha>@db:5432/<seu_banco>

# Variáveis do PostgreSQL (usadas pelo container 'db')
POSTGRES_USER=<seu_usuario>
POSTGRES_PASSWORD=<sua_senha>
POSTGRES_DB=<seu_banco>

# Variável do Redis (CRUCIAL para o RQ Worker - deve ser preenchida)
REDIS_URL=redis://redis:6379

# Variáveis de Configuração da Aplicação Flask (Essenciais)
SECRET_KEY=uma-chave-secreta-forte-e-aleatoria-aqui
# SUPER_ADMIN_EMAIL=op.almeida@hotmail.com  # Se necessário para o Flask

# A porta não precisa ser definida aqui, pois o docker-compose e o gunicorn definem (remover do .env base)
# PORT=10000