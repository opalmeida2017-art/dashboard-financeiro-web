# Variáveis de conexão do banco de dados (usadas pelo container web/worker)
# CORREÇÃO CRÍTICA: Mudar o host de 'localhost' para 'db'
DATABASE_URL=postgresql://<seu_usuario>:<sua_senha>@db:5432/<seu_banco>

# Variáveis do PostgreSQL (usadas pelo NOVO container 'db')
POSTGRES_USER=<seu_usuario>
POSTGRES_PASSWORD=<sua_senha>
POSTGRES_DB=<seu_banco>

# Variável do Redis (se você tiver um container Redis com o nome de serviço 'redis')
# REDIS_URL=redis://redis:6379

# Variáveis para o container 'web'
PORT=10000